--- a/arch/arm64/net/bpf_jit_comp.c
+++ b/arch/arm64/net/bpf_jit_comp.c
@@ -918,6 +918,16 @@
 	helper_gate_cap = cheri_perms_clear(
 		helper_gate_cap, CHERI_PERMS_WRITE);
 	helper_gate_cap = cheri_sentry_create(helper_gate_cap);
+#ifdef CONFIG_CBPF_ARRAY_AUTHORITY
+	pr_info("CBPF_SPATIAL_NATIVE_GATE target=0x%llx tag=%u sealed=%u base=0x%llx cursor=0x%llx length=%llu perms=0x%llx\n",
+		(u64)(unsigned long)bpf_cheri_helper_gateway,
+		(unsigned)cheri_tag_get(helper_gate_cap),
+		(unsigned)cheri_is_sealed(helper_gate_cap),
+		(u64)cheri_base_get(helper_gate_cap),
+		(u64)cheri_address_get(helper_gate_cap),
+		(u64)cheri_length_get(helper_gate_cap),
+		(u64)cheri_perms_get(helper_gate_cap));
+#endif
 
 	map_table_cap = cheri_tag_clear(ddc);
 	if (used_map_cnt) {
--- a/kernel/bpf/arraymap.c
+++ b/kernel/bpf/arraymap.c
@@ -72,6 +72,12 @@
 	    cheri_length_get(cap) != map->value_size ||
 	    cheri_perms_get(cap) != CBPF_ARRAY_DATA_PERMS)
 		return NULL;
+	pr_info("CBPF_SPATIAL_NATIVE_VALUE branch=reduced map_id=%u index=%u value_size=%u stride=%u values=0x%llx selected=0x%llx tag=%u sealed=%u base=0x%llx cursor=0x%llx length=%llu perms=0x%llx\n",
+		map->id, index, map->value_size, array->elem_size,
+		(u64)(unsigned long)array->value, (u64)value,
+		(unsigned)cheri_tag_get(cap), (unsigned)cheri_is_sealed(cap),
+		(u64)cheri_base_get(cap), (u64)cheri_address_get(cap),
+		(u64)cheri_length_get(cap), (u64)cheri_perms_get(cap));
 	return cap;
 }
 #endif
